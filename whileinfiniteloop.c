#include <stdio.h>

int main(){
    int a;
    scanf("%d",&a); //while loop checks the condition then executes the codes
    //infinte loop

    while(a>10){
        printf("%d\n",a);
        a++;
    }
    return 0;
}