#include <stdio.h>

int main(){
    // int marks[3]={ 34, 32,53};
    // for (int i=0;i<3;i++)
    // printf("the value scored by %d th student is %d \n",i+1,marks[i]);
    float marks[3]={ 34, 32,53};
    for (int i=0;i<3;i++)
    printf("the value scored by %d th student is %f \n",i+1,marks[i]);
    return 0;
}