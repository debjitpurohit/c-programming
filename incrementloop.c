#include <stdio.h>

int main(){
     int i=5;
     printf("the value of ++i is %d\n",i++); //++i mane prothome increment thn print and i++ mane first print thn increment
     printf("the value of i is %d\n",i); // ++i -->the value of i++ is 6 the value of i is 6
     i+=10; //--> increment by 10
     printf("%d\n",i);
     //i++ -->the value of i++ is 5 the value of i is 6
    // printf("the value of i-- is %d\n",i--); 
     //printf("%d",i);
     //--i -->the value of i-- is 4the value of i is 4
    // printf("the value of i is %d",i); // i-- -->the value of i-- is 5the value of i is 4

     return 0;
}

