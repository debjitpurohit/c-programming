#include <stdio.h>

int main(){
    int length,breadth;
    printf("what is the length of the rectangle \n");
    scanf("%d", &length);

    printf("what is the breadth of the rectangle");
    scanf("%d",&breadth);
     
    printf("the atrea of the rectangle %d",length*breadth);
    return 0;
}